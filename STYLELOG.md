# Style Log

## For You page
- Page background updated to `#f6f8fb` to match the light canvas in the screenshots.
- Featured selection card uses `#fef3d9` background, `#fde4ba` border, and 24px radius to mirror the highlighted hero tile.
- Sections separated with 56px vertical gaps for consistent breathing room.
- Book tiles employ 20px inner padding, 20px radius, and a subtle `0 20px 40px rgba(12, 35, 71, 0.06)` shadow to reflect the elevated cards in the reference design.

## Book detail page
- Converted layout to a two-column hero with max-width 260px cover and pastel background `#f6f8fb` to mirror the reference.
- Hero metrics use flex rows with 28px gaps and icons (FiStar/FiClock/FiBookOpen) to match the screenshot's stat chips.
- Primary CTA button uses deep navy `#022c41` fill with 10px radius; secondary outline reuses the same hue for consistency.
- Content sections use white cards (`#ffffff`) with 28px radius, `0 24px 40px rgba(12, 35, 71, 0.05)` shadows, and 32px padding for the summary/author blocks.

## Player page
- Lightened background to `#f6f8fb` and centered the page title with a slim divider as shown in the reference.
- Introduced a gated card with 32px radius, `#ffffff` fill, and `0 30px 60px rgba(12, 35, 71, 0.1)` shadow to replicate the login prompt.
- Auth CTA adopts the bright green `#2bd97c` pill button while audio/player panels reuse white cards with 32px radii and soft blue borders.
- Authenticated layout uses a 320px cover column with elevated shadow plus pill-shaped meta badges in `#eef4ff` to match the screenshot's rhythm.

## Choose plan page
- Replaced the dark gradient with a deep navy hero arc and large 120px rounded base to mirror the curved header.
- Highlights and plan cards now sit on `#f5f8ff` canvas with white 24–32px radius cards, pastel blue borders, and soft drop shadows.
- Billing toggle restyled as a pill-switch on `#e6f1ff`, with the active option elevated to mimic the reference.
- CTA buttons use the green gradient `#0f9f59 → #2bd97c`, while the yearly plan card gets a `#2bd97c` outline and badge for the “Best value” treatment.

